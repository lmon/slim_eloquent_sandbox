Slim Eloquent Integration

Create Slim app using composer
Include Illuminate
Use Capsule ( later am able to move away from Capsule and use straight up Eloquent )
Create a couple of sample models based on existing DB
Create relationships through model classes

Create demos for select, select where, select order by, select with relations, select particular columns, limits. create table, do inserts
